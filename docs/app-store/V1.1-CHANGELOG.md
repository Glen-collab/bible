# Version 1.1 — pending changes

Everything on `main` that is **not** in App Store build 1.0 (2). Add to this file as
work lands, so the "What's New" text and the release checklist write themselves.

**Baseline:** build 1.0 (2) = commit `7233e8e`, uploaded 2026-07-29.
**Status: 1.0 was APPROVED. Build 1.1 (3) uploaded 2026-09-06** — the Kids-Category
verification was re-run against the shipped file set that morning and all four answers
still hold. Waiting on the App Store Connect version record and submission.
**These changes are live on the web already** — GitHub Pages serves from the repo root,
so `glen-collab.github.io/bible/` has them now, at `?v=83`. Only the iOS binary is
waiting. **Tested in the browser by Glen 2026-08-09: the stone lands on the doorway and
Ada's track explanation reads correctly.**

---

## Commits in this release

| Commit | What |
|---|---|
| `9b6d524` | Empty Tomb coordinates retuned to the backdrop |
| `ab8fba2` | Railed stone explains discarded coords (first attempt) |
| `a1c1718` | …and the rung celebration no longer swallows that explanation |
| `418ca5e` | Asset version bumped to `?v=83` so all of the above reach browsers |
| `e1470d3`, `1c014c5`, `a940909` | Docs only — no app code |
| `440bbe2` | Nudge arrows move a quarter square, not a whole one |
| *(this commit)* | Locked badges say what they are for; the win screen names the last one missing |

---

## Changes since build 1.0 (2)

### The Empty Tomb scene now matches its artwork — `9b6d524`

The rail and rung coordinates were left over from an older layout, so pieces landed off
the tomb art. Pieces anchor by their **top-left corner** and span `DEFAULT_SIZE` cells,
so the on-screen center is `col + (size-1)/2`; measured against the doorway on
`assets/scenes/tomb.png` (~70% across, 60% down):

| Piece | Was | Now | Lands at |
|---|---|---|---|
| stone (sealed) | (2,1) | **(5,3)** | 72% / 63% — on the door |
| stone (open) | (0,1) | **(2,3)** | 34% / 63% — flat roll onto the path |
| jesus | (4,3) | **(5,3)** | 69% / 58% — in the doorway the stone left |
| angel | (2,2) | **(4,4)** | 53% / 71% — ground level before the mound |

`women` at (1,4) was already correct (20% / 77%) and is unchanged. The rail was kept
deliberately — the sideways roll is the story beat, rung 0's no-argument
`place("stone")` only works because the rail supplies `home`, and `open` stays on the
same row so `move("stone","left")` and the left-opens logic at `src/workshop.js:384`
still hold. Pulling `open` back to column 2 also stops the stone overlapping the risen
Jesus.

Why this mattered more than it looks: **`emptyTomb` is the only `freeBuild:false` scene
in the app.** The other ten are free play, but here Ada dictates the exact numbers and
`itemAt` (`src/workshop.js:205`) checks them with zero tolerance — so a stale target
taught a wrong spot and then said "Perfect!"

### The railed stone explains itself — `ab8fba2`

`place()` on a railed piece overrode the typed coordinates and echoed back the rail's
home position, so a kid typing `place("stone", 1, 1)` read *"stone placed at 5, 3"* with
no explanation. Now, **only** when the typed coordinates differ from home, Ada says the
stone is too heavy to lift and rolls in a carved track, and the console echo reads
*"rolled to its place on the track at 5, 3."* The guided path is untouched — rung 0 and
the `aiPreview` line pass no coordinates or the matching ones, so both still get the
plain sealed hint.

**Follow-up fix (found by testing it in the app):** the first attempt printed the console
echo but Ada stayed silent. `emptyTomb` is `freeBuild:false`, so snapping the stone home
*satisfied rung 0*, `onRungComplete()` fired, and the message was discarded unread — the
kid typed the wrong numbers and Ada said *"🎉 You did it!"*, which is worse than the
original silence. `tutorSay()` replaces the panel rather than appending, so a second call
could not fix it either. The note now rides into `onRungComplete(lead)` and is prepended
in one call. Verified: wrong coords → explanation **and** celebration; correct or absent
coords → celebration only, no spurious lecture; no stale note leaking into the next
command.

### Docs — `e1470d3`

`PROJECT_STATUS.md` told the next reader to run a 600+ check test suite before pushing.
**No test file exists in the repo or in git history.** Replaced with what actually works
(node v24 is installed now, so the "no node in this env, use `jsc`" note was also stale)
plus the corner-anchor geometry rule. No app code involved.

---

### The nudge arrows move a quarter square — `440bbe2`

The d-pad in the selection bar stepped a full cell, which is the coarse jump it exists to
avoid — a piece sitting slightly out of true had no way to be brought into line. It now
steps `0.25`, and Ada explains the difference once per workshop: little arrows for little
moves, `move()` for whole squares.

Rendering was already fractional-safe (`positionEl` is arithmetic on a percentage), so the
work was making sure a fractional position cannot affect anything it should not. Each piece
now carries `cellCol`/`cellRow` — the square the kid **named** — beside `col`/`row`, where it
is **drawn**. Rung goals read the named square. That holds the line both ways: type the right
coordinates and then tidy the piece up, and the tick stays; type the wrong ones and the d-pad
cannot earn it for you.

A first attempt rounded the drawn position instead and failed both ways — two nudges make
half a square and round up to the next cell, and a wrong placement nudged within a quarter
would silently pass. Found by testing the arithmetic, not by reading it.

The nudge is deliberately **not** echoed as `move("name", 5.25, 4)`: `move()` rounds its
arguments, so a kid typing that back would watch the piece jump and reasonably conclude the
app lies to them.

### Locked badges explain themselves — *(this commit)*

A perfect playthrough finishes on **5 of 6, every time, in all 44 variants.** The detour
badge is awarded in exactly one place, inside `renderDetour()`, which only runs on a wrong
answer — so it cannot be earned without a mistake, and 6 of 6 needs one stop wrong and
another solved cleanly.

That is defensible design: the app's position is that a wrong turn is a gentle detour with a
lesson in it, and the badge is for coming back from one. What was not defensible is that it
was invisible. The badge sheet printed "Locked" and hid the description, so a careful player
saw a badge they could never earn and no way to learn why.

No mechanics changed. A locked badge now shows its description — which already *is* the
instruction — and when a finished case is missing exactly one badge, the win screen names it.
The detour gets its own wording, because "you did not make a mistake" is an odd thing to be
told off for.

## Verified in the browser (Glen, 2026-08-09)

- [x] **Tomb coordinates** — all four confirmed good on screen. The stone sits on the
      doorway, and the risen Jesus and the angel land where they should. Derived from
      measuring the backdrop plus the anchor math, then checked by eye.
- [x] **Ada's rail message** — the first attempt was silent (see the follow-up fix
      above); the corrected version explains the track and then celebrates.

## Still open

- [ ] Decide whether 1.1 also brings iPad support (`TARGETED_DEVICE_FAMILY = 1` today;
      iPad layout untested — this was the original reason 1.0 shipped iPhone-only).
- [ ] Nothing else — **the web side of 1.1 is complete and verified.** From here it is
      waiting on 1.0 approval, then the build steps below.

## Lesson recorded from this round

Three of these commits were live on Pages and **invisible in the browser** because
`index.html` still pinned `?v=82`. `curl` confirmed the deploy while the browser showed
old behavior — both true at once, and very easy to misread as a code bug. **Bump `?v=NN`
in the same commit as any `src/`, `data/`, or `styles/` change.** Also in
`PROJECT_STATUS.md`. Note this cannot affect the iOS app, where Capacitor bundles `www/`
into the binary.

## Draft "What's New"

> The Empty Tomb workshop now lines up with its artwork — the stone rolls across the
> doorway where it belongs, and the risen Jesus and the angel stand in the right places.
> Ada explains why the great stone can only be rolled and never lifted.
>
> The nudge arrows now move a piece a quarter of a square instead of a whole one, so a
> scene can be lined up exactly the way you want it.
>
> Badges you have not earned yet now tell you what they are for, and finishing a case one
> badge short says which one is still out there.

## Known warning, not blocking

`altool` reports on every upload:

> MinimumOSVersion too low. This app has a MinimumOSVersion of 13.0. Starting in Spring
> 2027, all iOS apps must have a MinimumOSVersion of 15.0 or later.

A **warning**, not an error — 1.1 uploaded fine. It has to be dealt with before Spring 2027.
Raising `IPHONEOS_DEPLOYMENT_TARGET` to 15.0 drops iOS 13 and 14 devices, which is a
shrinking sliver, and Capacitor 6 is happy either way. Do it in a release where there is time
to test, not on the way out of the door.

## Release checklist

1. `git pull` and confirm the tomb scene renders correctly in a browser first.
2. Re-run the Kids-Category verification in `KIDS-CATEGORY-ANSWERS.md` — the four
   answers must still be "no" (they are, as of 2026-08-09, on current `main`).
3. Bump **both** `MARKETING_VERSION` → `1.1` and `CURRENT_PROJECT_VERSION` → `3` in
   `ios/App/App.xcodeproj/project.pbxproj` (**two places each**, the Debug and Release
   configs — lines ~357/362 and ~378/383). ASC rejects duplicate build numbers.
4. `npm run build:web && npx cap sync ios`
5. Archive, export, validate, upload — full commands in `APP_STORE.md`.
6. Create the 1.1 version in App Store Connect, attach the build, paste What's New,
   answer the Kids-Category questions again from `KIDS-CATEGORY-ANSWERS.md`, submit.

**Do not start any of this until 1.0 is approved.** Attaching a new build to the version
currently under review would abandon the in-flight submission and restart the queue.
